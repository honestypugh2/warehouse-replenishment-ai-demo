"""Foundry orchestration package.

In MOCK_MODE these modules run the same agent logic locally so the demo is
self-contained. In production they map to Microsoft Agent Framework agents and
orchestrations hosted in Foundry Agent Service.
"""
